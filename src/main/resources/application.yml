server:
  forward-headers-strategy: framework

spring:
  datasource:
    url: "jdbc:mysql://localhost:3306/buonappetito"
    username: "root"
    password: "test1234"
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    database-platform: org.hibernate.dialect.MySQL8Dialect
    hibernate:
      ddl-auto: create
    show-sql: true